# Skill: `design-tokens-and-theme` (Bundled with `ux-architect`)

Dedicated standalone branch for `design-tokens-and-theme`.

## Installation
```bash
mkdir -p .agents/skills/design-tokens-and-theme
git clone -b design-tokens-and-theme https://github.com/krax88/agy_agents.git .agents/skills/design-tokens-and-theme
```
