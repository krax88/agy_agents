# Skill: `analyze-large-audio` (Bundled with `senior-ai-advisor`)

Dedicated standalone branch for `analyze-large-audio`.

## Installation
```bash
mkdir -p .agents/skills/analyze-large-audio
git clone -b analyze-large-audio https://github.com/krax88/agy_agents.git .agents/skills/analyze-large-audio
```
