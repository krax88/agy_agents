# Skill: `enterprise-transformation-advisor` (Bundled with `senior-ai-advisor`)

Dedicated standalone branch for `enterprise-transformation-advisor`.

## Installation
```bash
mkdir -p .agents/skills/enterprise-transformation-advisor
git clone -b enterprise-transformation-advisor https://github.com/krax88/agy_agents.git .agents/skills/enterprise-transformation-advisor
```
